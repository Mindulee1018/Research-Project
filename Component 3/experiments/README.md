# Experiments

Track experiment configurations and summaries for reproducible model comparisons.

Recommended layout:

- `configs/` for training/eval configs
- `runs/` for small metadata outputs (not heavy artifacts)
- `reports/` for experiment comparison summaries
