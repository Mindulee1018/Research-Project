# Tests

Test areas:

- `backend/` API and service tests
- `frontend/` UI and interaction tests
- `training/` pipeline sanity tests

Phase 1 keeps directory scaffolding and CI smoke checks. Detailed tests are added in later phases.
